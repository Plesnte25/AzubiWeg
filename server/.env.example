DATABASE_URL="postgresql://companion:companion@localhost:5433/companion"
JWT_SECRET="generate-a-long-random-string"
PORT=3000

# Optional: enables Claude-powered OCR for photo notes (reads handwriting).
# Without it, images fall back to local tesseract (printed text only).
ANTHROPIC_API_KEY=
