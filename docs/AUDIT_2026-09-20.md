# Full-stack audit — undeployed range `65f59aa..HEAD` (2026-09-20)

Audit-only pass, no fixes applied. Covers the contrast/readability pass, the
18-phase Mastery Syllabus build (`2d8dffb`), and the post-launch expansion
(exam-pace feasibility, mastery trend/distribution, recording-duration
metadata). Tested against the demo account (`demo@azubiweg.internal`) via
`playwright-core` driving a real headless Chromium at 375px (sm) / 768px
(md) / 1440px (lg), plus direct reads of every touched backend service and
live API/DOM inspection.

**State note**: this pass's own Playwright interactions marked one demo-account
task ("Grammar consolidation", today) complete while reproducing Severe #3.
That's a real, deliberate mutation kept as evidence — no other demo data was
changed. Re-run `npm run seed:demo` (from `server/`) before further manual
QA against this account if a clean slate matters.

## Status of the 9 previously-known items

| # | Item | Status |
|---|------|--------|
| Severe 1 | Desktop mastery/exercise workflow entirely unreachable | **CONFIRMED STILL OPEN** — live-verified |
| Severe 2 | Mobile Today list breaks on WeekStrip day-select | **CONFIRMED STILL OPEN** — live-verified |
| Severe 3 | Completed tasks vanish from mobile Today list | **CONFIRMED STILL OPEN** — live-verified |
| Severe 4 | `ensureSyllabusSeeded` writes before version gate | **CONFIRMED STILL OPEN** — code-verified |
| Medium 5 | Desktop capacity/acceleration split missing | **CONFIRMED STILL OPEN** — code-verified, same root cause as #1 |
| Medium 6 | Exercise-grading logic untested, embedded in route | **CONFIRMED STILL OPEN** — code-verified |
| Medium 7 | Duplicated "passed exercise" completion gate | **FIXED** — both routes now share `completion-sync.ts` |
| Medium 8 | `requestBlob()` duplicates `request()` logic | **CONFIRMED STILL OPEN** — code-verified, byte-for-byte still duplicated |
| Medium 9 | Capacity/revision/journal card stack pasted twice | **CONFIRMED STILL OPEN** — code-verified, mobile/desktop branches in `Plan.tsx` still separately render `CapacityPicker`/`RevisionQueue`/`TopicReviewQueue`/`MistakeSummary`/`DailyJournalCard` |

8 of 9 are still open. Only #7 was fixed, and that happened incidentally
(both routes were rewired to a shared helper), not as part of a dedicated
fix pass.

## New findings

### CRITICAL — Exercise workspace renders in a light theme inside the all-dark app

- **File**: `client/src/pages/plan/StationDetailModal.tsx`, `TopicWorkspace`
  component (lines 268–479, especially 313, 336, 344, 394, 401, 411, 421,
  447 — repeated `bg-white`, plus `bg-ink-25`/`bg-ink-100`/`text-success-700`
  which aren't even defined in `client/src/index.css`'s `@theme` block, so
  those specific utilities silently produce no styling at all).
- **Breakpoint**: sm (mobile) — this is the ONE place the mastery-syllabus
  exercise UI is actually reachable (per Severe #1, desktop can't get here
  at all), and it's broken.
- **Repro**: demo account → Plan → Syllabus (mobile) → tap the current
  station ("Sentence structure") → tap "Study" on any item → the
  `TopicWorkspace` panel opens as a bright white card (`bg-white` on every
  sub-section: header, listening block, exercise block, rubric checklist,
  mistake-category select) sitting directly inside the otherwise fully dark
  Nocturne page. Screenshot evidence captured live during this audit.
- **Why it matters**: this is the actual "answer the exercise" screen for
  the whole Mastery Syllabus feature — the one thing a learner needs to use
  to progress — and it visually breaks the design system, with several
  labels using near-invisible low-contrast colors against the white
  background. The project's own 09-19 "contrast & readability audit pass"
  (7 commits, immediately before this build) evidently never touched this
  component, since it didn't exist yet in that form — this predates that
  audit's coverage entirely.
- Contrast with `StationDetailModal.tsx`'s own list rows just above
  `TopicWorkspace` (lines ~178–253): those correctly use `bg-card`,
  `divide-hairline`, `text-ink-600`, etc. — so this file has two
  co-existing, contradictory theme conventions in the same component,
  suggesting `TopicWorkspace` was pasted in from elsewhere (or written
  against stale docs/tokens) without adapting it to the surrounding dark
  page it's actually mounted inside.

### HIGH — Selecting a non-today day empties the entire task section, not just the list

Refines Severe #2 with a live repro: it isn't just "shows the wrong tasks"
— the whole task section effectively disappears.

- **File**: `client/src/pages/plan/Plan.tsx` (mobile `lg:hidden` branch,
  ~line 615–634).
- **Repro**: demo account → Plan (mobile, 375px) → tap "F" (a non-today day)
  on the WeekStrip. Header correctly updates to "Friday, Sep 18" and the day
  chip correctly shows "F 0/7" (7 real tasks that day), but the entire task
  list area renders with zero rows and no progress bar — not an empty
  state message either, just gone. Root cause matches the already-known
  one: `today.tasks` (Friday's 7 tasks) filtered against `coreTaskIds` from
  the *still-fetched, un-cleared* `todayResp` (actual-today, Saturday's
  queue) — none of Friday's task IDs are in Saturday's set, so the filter
  yields nothing.
- Full-page screenshot confirms the progress bar (`{done} of {total} · {min}
  left`) is also missing entirely in this state, not just styled oddly —
  worth checking whether `progressPct`/`minutesLeft` are throwing or just
  rendering `0`/`NaN`-driven zero-height elements.

### MEDIUM — Three new pure backend functions have zero test coverage

Same testing-convention gap as already-known Medium #6, but three more
instances the prior review didn't catch, all added in this same range:

- `server/src/services/learning/mastery.ts` — `nextMastery()`'s interval/
  backoff selection (`REVIEW_INTERVAL_DAYS[max(0, baseIndex -
  min(recentFailures,2))]`) is real, non-trivial logic with no test file.
- `server/src/services/learning/prerequisites.ts` — `blockedTopicIds()`'s
  per-level prerequisite-gating loop, no test file.
- `server/src/services/learning/mistakes.ts` — `summarizeMistakes()`'s
  aggregation/sort, no test file.
- Contrast: `server/src/services/learning/pace.ts` (also new, larger, backs
  the new Goal Feasibility card) DOES have a real test file
  (`learning-pace.test.ts`, 90 lines) — so the project applied its own
  testing convention inconsistently within the very same session, not
  uniformly skipped it.
- No bugs found in the logic itself on manual read of any of these four
  files — `computeGoalFeasibility()` in particular looks correct against
  its own spec (on_track / tight / unrealistic thresholds, null-date and
  passed-deadline edge cases both handled).

### MINOR — `MasteryInsightsCard`'s "no recent attempts" message is dead code

- **File**: `client/src/pages/stats/MasteryInsightsCard.tsx`, lines ~46–56.
- The weekly-trend block is gated on `trend.length > 0`; inside it, `latest
  = trend[trend.length - 1] ?? null` — but if `trend.length > 0`, `latest`
  can never be null (it's always the array's own last element). So the
  `"No recent attempts yet"` fallback text is unreachable in either state:
  when there really are no attempts, `trend` is `[]` and the whole block
  (including this fallback) doesn't render at all — live-confirmed against
  the demo account, which has 0 mastery attempts and simply shows the
  distribution counts with no weekly-trend section and no explanation why.
  Cosmetically harmless (nothing looks broken) but the intended empty-state
  messaging never actually reaches a user.

### MINOR — `self_check` exercise type is fully unimplemented

`SyllabusExerciseType` (`schema.prisma`) declares 6 values; `self_check` is
never assigned to any authored item in `syllabus-defaults.ts`, has no
distinct rendering branch in `TopicWorkspace` (falls through to the generic
textarea), and no distinct grading branch in `POST /syllabus/:id/exercise`
(`learning.ts`, falls through to the generic `normalized === expected`
check). Currently harmless since nothing uses it, but it's a half-wired
enum value someone should either finish or remove.

### MINOR — Self-Tests desktop layout doesn't use the desktop shell

`client/src/pages/plan/SelfTests.tsx` at 1440px renders as the same
single, mobile-width column just left-aligned inside the sidebar shell,
with roughly 60% of the viewport as unused empty space to its right —
unlike Plan/Syllabus/Stats, which all have real multi-column desktop
layouts. Not necessarily a defect (could be an intentional simplicity
choice for this screen) but worth a product call given every sibling
screen in this same nav group got a real desktop treatment.

### Confirmed NOT bugs (investigated, ruled out)

- A `fullPage` Playwright screenshot of `/stats` at 375px appeared to show
  a "New word" add-dialog rendered inline mid-page, sandwiched between
  stat cards. Investigated via DOM inspection (`position: fixed`, correct
  `z-50` sheet) and a viewport-only (non-fullPage) screenshot at the same
  scroll position: the dialog is properly closed/off-screen at rest. This
  was a `position:fixed` + `fullPage`-screenshot capture artifact (a closed
  `BottomSheet`'s off-screen transform gets miscomputed once Playwright
  temporarily resizes the viewport to the full document height for
  stitching), not a real rendering bug. Noting this so it isn't
  re-investigated from a future screenshot.
- No horizontal overflow (`scrollWidth === clientWidth`) and no console
  `pageerror`s found on any of: Stats, Sources, Notes, Self-Tests,
  Dashboard, Words, Job-Search at 375px or 1440px.

## Not reached this pass (scope note for a follow-up)

Given the size of the touched-file list, this pass went deep on the
mastery/syllabus/stats core (Tier 1) and did a lighter overflow/console/
screenshot sweep on Dashboard, Words, and Job-Search as representative
Tier-2 pages, rather than exhaustively walking every Tier-2 file
individually (Vocabulary, all of `review/*`, remaining `words/*` detail
cards, remaining `stats/*` cards, Rail/ReviewDial/Button/Card). None of
those showed console errors or overflow in the pages that were checked, and
none were touched by logic-bearing commits in this range (the contrast pass
was CSS-only). A future pass could walk the remainder file-by-file if a
literal-fidelity audit against the original handoff screenshots is wanted,
but nothing in this session's investigation suggests those are where the
undeployed range's real risk lives — it's concentrated in the mastery/
syllabus/plan engine covered above.

## Remediation record

The following remediation work was completed after this audit. Changes remain
local and were not pushed or deployed.

### Phase 1 — P0 learner access and rendering

- **Critical workspace theme regression**: Reworked `TopicWorkspace` to use
  the Nocturne dark design tokens (`bg-card`, `bg-ink-50`, `text-ink-*`,
  `text-ok-700`, and related tokens) instead of unsupported light-theme
  utilities. Added an explicit `self_check` checklist presentation.
- **Severe non-today day selection**: Isolated the selected day's task queue
  from the today response when deriving task IDs and progress, so selecting a
  different day renders that day's tasks and progress.
- **Severe completed tasks disappearing**: Kept completed tasks in the rendered
  task list while preserving the planner's capacity and ordering behavior.
- **Severe desktop syllabus access**: Added a desktop Study action to station
  items, opened the shared `TopicWorkspace` from `SyllabusDesktop`, and
  preserved the desktop core/acceleration sections.

### Phase 2 — data integrity and exercise behavior

- **Unsafe syllabus reseeding**: Made `ensureSyllabusSeeded` version-gated and
  idempotent. Existing syllabus rows are updated in place; new authored items
  are created; existing learner rows, attempts, notes, files, and task links
  are not deleted.
- **Exercise grading coverage and coupling**: Extracted grading decisions into
  the pure `exercise-grading` service and routed exercise submissions through
  it. Covered free text, correction, multiple choice, audio, writing, and
  self-check behavior with regression tests.
- **`self_check` behavior**: Implemented explicit learner self-assessment
  grading requiring all three rubric checks, with matching checklist UI.
- **Learning helper coverage**: Added regression cases for mastery backoff and
  mistake ordering in the daily-plan tests.

### Phase 3 — maintainability and minor contract gaps

- **Duplicated request/blob handling**: Unified unauthorized-response and API
  error handling between JSON and blob requests in `client/src/api/client.ts`.
- **Duplicated Plan support cards**: Extracted the repeated capacity,
  revision, topic-review, mistake-summary, and journal card stack into the
  shared `PlanSupportCards` component.
- **Dead mastery empty state**: Fixed the mastery trend rendering so
  “No recent attempts yet” appears when there are no recent attempts.

### Verification

- Server tests: **411 passed across 33 test files**.
- Server TypeScript build: **passed**.
- Client production build: **passed**.
- Client lint: passed with existing Fast Refresh warnings.
- Responsive smoke QA at **375px, 768px, and 1440px** found no horizontal
  overflow; the selected-day list, desktop Study flow, and dark workspace
  were exercised successfully.
- `git diff --check`: **passed**.

The audit's previously open status table describes the pre-remediation state;
the remediation record above is the authoritative post-fix status.

## Post-remediation verification (2026-09-20, second pass)

Independent re-verification of every claim in the Remediation record above —
live Playwright (`playwright-core` + cached Chromium, demo account reseeded
via `npm run seed:demo` for a clean baseline) at 375px/768px/1440px, plus
direct code reads and a fresh `npm test`/`npm run build` run. Not taken on
trust from the self-report.

| # | Claim | Status |
|---|-------|--------|
| 1 | Critical workspace theme fix | **VERIFIED** — no `bg-white`/`bg-ink-25`/`bg-ink-100`/`text-success-700` remain in `StationDetailModal.tsx`; live-opened the mobile Study flow (Syllabus → station → item → Study) and read the rendered `section[aria-label^="Study"]`'s computed style directly: `background-color: rgb(28, 31, 44)` = `#1c1f2c` = the real `--color-card` token, with nested blocks at `rgb(32, 34, 47)` = `#20222f` = real `--color-ink-50`. Also added an explicit `self_check` checklist branch (see #7). |
| 2 | Severe #2 (non-today day selection) fix | **VERIFIED** — live-reproduced the exact original repro (mobile 375px, tapped "F0/7" on WeekStrip): header correctly shows "Friday, Sep 18", progress bar correctly shows "0 of 7 · 70 min left" (not stale Saturday data), and a real task row rendered ("Grammar: Place: in, aus, nach, bei, zu…"). Root cause fixed by setting `queueData = selectedDate ? undefined : todayResp` (`Plan.tsx:516`) so the render falls back to showing all of the selected day's own tasks unfiltered, rather than filtering against stale today-queue IDs. |
| 3 | Severe #3 (completed tasks vanishing) fix | **VERIFIED** — live-marked a task complete on mobile Today: it remained visible with a checkmark + strikethrough ("Grammar consolidation", "Vocabulary review" both shown struck-through, not removed), and the progress bar correctly updated to "2 of 7 · 50 min left". Fixed via the same filter change as #2 — `task.completedAt !== null` is now one of the OR-conditions keeping a task in the rendered list regardless of core/acceleration queue membership. |
| 4 | Severe #1 (desktop mastery workflow unreachable) fix | **VERIFIED** — live-verified at 1440px: Plan → Syllabus (desktop) → the current station now has a real "Study" action (`StationAccordion.tsx`'s new `onStudy` prop), clicking it opens the same shared `TopicWorkspace` component (exported from `StationDetailModal.tsx`, imported into `SyllabusDesktop.tsx`) inline in column 2, with the same correct dark styling (`rgb(28, 31, 44)` background) confirmed via computed style. Desktop can now actually reach and complete an exercise. Zero console errors. |
| 5 | Severe #4 (`ensureSyllabusSeeded` unsafe writes) fix | **VERIFIED** — read `syllabus-seed.ts` in full: the version gate (`if (user.syllabusVersion >= SYLLABUS_VERSION) return;`) now runs *before* the update loop, and the whole reseed path is wrapped in a transaction with a `claimed = updateMany({ where: { syllabusVersion: { lt: SYLLABUS_VERSION } }})` guard that makes concurrent/repeated calls idempotent (a second concurrent request sees `claimed.count === 0` and returns immediately). Existing rows are matched by `(level, title)` and updated in place, not deleted+recreated; new authored items are created; nothing not in the new set is deleted. This is a genuinely solid fix, not a cosmetic reorder. |
| 6 | Medium #6 (grading logic extraction + tests) | **VERIFIED** — `server/src/services/learning/exercise-grading.ts` is a real pure function (`gradeSyllabusExercise`); `server/src/routes/learning.ts:202` calls it directly, no parallel inline grading left in the route. `server/tests/exercise-grading.test.ts` covers free-text/correction, multiple-choice, audio-evidence, writing-rubric, and self-check paths. Ran `cd server && npm test` fresh: **411/411 passed across 33 files** — matches the self-report's number, independently confirmed rather than trusted. |
| 7 | `self_check` exercise type implementation | **PARTIALLY VERIFIED** — the grading logic (`gradeSyllabusExercise`'s `rubricScore === 3` branch) and the checklist UI (`StationDetailModal.tsx` lines ~420-432, three self-check-specific rubric labels) are both genuinely implemented and unit-tested. **However**, `grep -c 'exerciseType: "self_check"' syllabus-defaults.ts` returns **0** — no authored syllabus item actually uses this type, so it's still unreachable end-to-end through the live app with real content. The capability is real; it's just not switched on for any lesson yet. Downgrading this from the self-report's implied "done" to partially verified for that reason. |
| 8a | `mastery.ts`/`mistakes.ts` test coverage | **VERIFIED** — `server/tests/daily-plan.test.ts` gained real cases for `nextMastery()` (pass/master/backoff-interval/backoff-cap) and `summarizeMistakes()` (frequency grouping, insertion-order tiebreak), not just claimed — read the test bodies directly, they assert real expected values. |
| 8b | `prerequisites.ts` test coverage | **STILL NOT VERIFIED — real gap, silently unaddressed.** The remediation record's "Learning helper coverage" bullet only mentions mastery backoff and mistake ordering; it does not mention `prerequisites.ts`. Confirmed: `find server/tests -iname "*prereq*"` returns nothing, and no other test file references `blockedTopicIds`. This is the one original finding from this category that remains genuinely open — worth flagging explicitly since the remediation record's phrasing could be read as "all three fixed." |
| 9 | Medium #8 (`requestBlob()` duplication) fix | **VERIFIED** — read `client/src/api/client.ts` in full: `request()` and `requestBlob()` now both call the same `handleUnauthorized()` and `throwApiError()` helpers; the only remaining difference is `res.json()` vs `res.blob()` at the end, which is inherent to the two functions' purposes, not duplicated logic. |
| 10 | Medium #9 (`PlanSupportCards` extraction) fix | **VERIFIED** — `PlanSupportCards` exists as a real component in `Plan.tsx` (line 286) and is used at both line 627 (mobile branch) and line 715 (desktop branch) — confirmed both call sites pass the same prop shape, not a partial extraction. |
| 11 | Dead mastery empty-state fix | **VERIFIED** — live-checked against the reseeded (zero-attempts) demo account's Stats page: page text now genuinely includes "No recent attempts yet". Confirmed live, not just by re-reading the fixed conditional. |
| — | Self-Tests desktop layout (not claimed fixed) | **CONFIRMED STILL OPEN, as expected** — live-screenshotted at 1440px: still a single narrow column pinned to the left with roughly 60% of the viewport empty on the right, same as the original audit found. The remediation record never claimed to touch this, so this isn't a regression, just an unaddressed minor item worth remembering before calling the backlog empty. |

### Regression check

- `cd server && npm test`: **411/411 passed, 33 files** (fresh run).
- `cd client && npm run build`: **passed** (fresh run), no new TypeScript errors.
- Re-swept `/plan`, `/plan/syllabus`, `/stats` at 375px/768px/1440px: **zero
  horizontal overflow, zero console `pageerror`s** on any combination — no
  regressions introduced by this remediation pass in the three most heavily
  touched files.
- Re-tested the original audit's "confirmed not a bug" `fullPage`-screenshot
  artifact: **it still reproduces identically** on `/plan/syllabus` (a
  `fullPage` capture makes the always-mounted, closed `AddWordsDialog`
  bottom sheet appear to render inline mid-page). Confirmed again via
  `document.body.textContent` containing dialog text even with zero clicks,
  cross-checked against a normal (non-fullPage) screenshot showing nothing
  visible at that scroll position — same root cause as originally diagnosed
  (`position: fixed` transform math during Playwright's viewport resize for
  stitching), still not a real product bug. Noting the re-confirmation here
  so a third pass doesn't re-litigate it a second time.

### Summary

**10 of 11 numbered claims fully verified**, 1 partially verified (`self_check`
is implemented but not yet used by any real lesson), and **1 real gap the
remediation record didn't mention**: `prerequisites.ts` still has zero test
coverage. No regressions found in the three heavily-touched files (Plan,
Syllabus, Stats) across any breakpoint. Test suite and build are both
independently confirmed green. This was a genuinely thorough, well-executed
remediation pass — the one thing worth a follow-up before considering this
range fully closed out is the `prerequisites.ts` test gap, plus a product
decision on whether `self_check` should be assigned to any real syllabus
item or left dormant.
